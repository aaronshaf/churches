#!/bin/sh

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
BOLD='\033[1m'
RESET='\033[0m'

echo "${BLUE}${BOLD}🚀 Running pre-push checks...${RESET}"

echo "${BLUE}🔁 Running CI parity suite (PR)...${RESET}"
bun run ci:pr || {
  echo "${RED}❌ CI parity checks failed${RESET}"
  exit 1
}

echo "${GREEN}${BOLD}✅ All pre-push checks passed!${RESET}"
