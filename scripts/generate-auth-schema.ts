// This script is deprecated - auth tables are now created via Drizzle migrations
// Run `bun db:migrate` to apply all migrations including auth tables

console.log('Auth tables are now created via Drizzle migrations.');
console.log('Run `bun db:migrate` to apply all migrations.');
process.exit(0);
